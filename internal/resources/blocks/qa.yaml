name: qa
description: Write tests that capture intended behavior
params:
  max_turns: {default: "30"}
  prompt: {default: "prompts/feature/qa.md"}
entry: qa
exits: [done]
states:
  - name: qa
    description: Write tests capturing intended behavior
    prompt: ${prompt}
    agent:
      max_turns: ${max_turns}
    next: $done
