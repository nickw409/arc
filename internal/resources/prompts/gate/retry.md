## Previous Attempt (attempt {{.Attempt}} of {{.MaxAttempts}})
The previous session exited but the gate did not pass.

Gate output:
```
{{.GateOutput}}
```

Changes made so far (preserved in your working directory):
```
{{.DiffSummary}}
```

Fix the failing gate checks. Build on existing changes — do not start over.
