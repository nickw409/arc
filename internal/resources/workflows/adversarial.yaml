name: adversarial
version: 1
description: Adversarial testing - implement freely, adversary breaks it, fix until convergence

pipeline:
  - block: impl
    params: {max_turns: "45"}
  - block: adversary-loop
    params: {max_rounds: "3", max_turns: "30"}

terminal_states: [complete, blocked]
