name: audit
version: 1
description: Adversarial audit - adversary finds bugs, then impl fixes them

pipeline:
  - block: adversary
    params:
      max_rounds: "5"
      max_turns: "30"
  - block: impl
    params:
      max_turns: "45"

terminal_states: [complete, blocked]
