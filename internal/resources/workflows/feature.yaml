name: feature
version: 3
description: Implement a feature and harden it with adversarial review

pipeline:
  - block: act
    name: impl
    params:
      prompt: "prompts/feature/impl.md"
      max_turns: "200"
      timeout: "3600"
  - block: adversary
    name: check
    params: {max_turns: "30"}
    route:
      bugs_found: impl
      no_bugs_found: complete

terminal_states: [complete, blocked]
